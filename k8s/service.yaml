apiVersion: v1
kind: Service
metadata:
  name: userservice-service
spec:
  type: ClusterIP
  selector:
    app: userservice
  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 8888